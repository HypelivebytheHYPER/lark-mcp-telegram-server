services:
  - type: web
    name: lark-mcp-telegram-server-test
    env: python
    buildCommand: pip install fastapi uvicorn python-dotenv
    startCommand: python test_minimal.py
    plan: starter
    envVars:
      - key: PORT
        value: 10000